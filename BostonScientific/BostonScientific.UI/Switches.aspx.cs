//using BostonScientific.DAL.Interfaces;
//using BostonScientific.DAL.Metodos;
using System;

namespace BostonScientific.UI
{
    public partial class Switches : System.Web.UI.Page
    {
  /*      private ITools _tools;
        private IPanels _panels;

        public Switches()
        {
            _tools = new MTools();
            _panels = new MPanels();
        }

        protected void Page_Load(object sender, EventArgs e)
        {
            //var IdPanel = Request.QueryString["P"];
            var IdPanel = "xcUhOJzCGeI=";
            
            var res = _panels.GetPanelInfo(IdPanel);

            lb_IdPanel.Text = string.Format("Código del panel: {0}", _tools.Capitalize(_tools.Decrypt(res[0].IdPanel)));
            lb_Description.Text = string.Format("Descripción: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Description)));

            lb_Modelo.Text = string.Format("Modelo: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Model)));
            lb_Bus.Text = string.Format("Bus: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Bus)));
            lb_Main.Text = string.Format("Main: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Main)));
            lb_SpacesAvailable.Text = string.Format("Espacios Disponibles: {0}", _tools.Decrypt(res[0].SpacesAvailable));

            lb_Location.Text = string.Format("Ubicación: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Area)));
            lb_From.Text = string.Format("From: {0}", _tools.Capitalize(_tools.Decrypt(res[0].From)));
            lb_Comments.Text = string.Format("Comentarios: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Comments)));

            lb_Voltage.Text = string.Format("Voltaje: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Voltage)));
            lb_Phases.Text = string.Format("Fases: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Phases)));
            lb_Threads.Text = string.Format("Hilos: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Threads)));
            lb_Frequency.Text = string.Format("Frecuencia: {0}", _tools.Capitalize(_tools.Decrypt(res[0].Frequency)));
        }

        public void prueba()
        {
            var num = 1;
            for (int i = 0; i < 21; i++)
            {
                Response.Write("<tr>" +
                            "<td class='text-center'>" + num +"</td>"+
                            "<td class='text-center'>Disponible</td>" +
                            "<td class='text-center'>0</td>" +
                            "<td class='text-center'>0</td>" +
                            "<td class='text-center'>"+
                                "<asp:Button class='btn btn-primary' ID='btnCrear' runat='server' Text='Ver' />"+
                            "</td>"+
                        "</tr>");
                num = num + 2;

            }
        }

        public void prueba1()
        {

            var num = 2;
            for (int i = 0; i < 21; i++)
            {
                Response.Write("<tr>" +
                            "<td class='text-center'>" + num + "</td>" +
                            "<td class='text-center'>Disponible</td>" +
                            "<td class='text-center'>0</td>" +
                            "<td class='text-center'>0</td>" +
                            "<td class='text-center'>" +
                                "<asp:Button class='btn btn-primary' ID='btnCrear' runat='server' Text='Ver' />" +
                            "</td>" +
                        "</tr>");
                num = num + 2;

            }
        }
    */}
}